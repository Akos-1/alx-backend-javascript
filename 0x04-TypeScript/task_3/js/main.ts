// main.ts
import { RowID, RowElement } from './interface';

// Example usage of RowID and RowElement
const rowID: RowID = 1;
const row: RowElement = {
  firstName: "John",
  lastName: "Doe",
  age: 30
};

console.log("Row ID:", rowID);
console.log("Row Element:", row);
